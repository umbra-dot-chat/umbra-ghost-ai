# Ghost AI Bot — Server Credentials
# Copy this file to .credentials and fill in real values.
# NEVER commit .credentials to git.

# SSH access to bot.umbra.chat (GPU server)
SSH_HOST=149.28.xx.xx
SSH_USER=root
SSH_PASS=your-server-password-here

# Ollama API (runs on the same server)
OLLAMA_URL=http://localhost:11434

# Relay server the bot connects to
RELAY_URL=wss://relay.umbra.chat/ws
